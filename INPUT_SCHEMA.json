{
  "title": "Input schema for qwiksilva/run-tasks actor.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tasks": {
      "title": "Tasks",
      "type": "array",
      "description": "Enter the tasks you want to run.",
      "editor": "stringList"
    },
    "bubbleEndpoint": {
      "title": "Bubble.io Endpoint",
      "type": "string",
      "description": "URL for where you want to send the data to. Leave blank if you do not want to send data to an API.",
      "editor": "textfield"
    },
    "apiEndpoint": {
      "title": "API Endpoint",
      "type": "string",
      "description": "URL for where you want to send the data to",
      "editor": "textfield"
    }
  },
  "required": []
}
